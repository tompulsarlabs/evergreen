# 2026-08-29

## Watchlist sync

`search_repositories user:tompulsarlabs` shows **15 repos** (up from 14), 0
forks, 0 archived. New: `yeva`, created 2026-08-28 09:25 UTC — matches the
local-WIP scanner's "Yeva" entry (private, `main`, first scan 2026-08-28).
`config.yml` exclude list still empty. Added `tompulsarlabs/yeva` to the
watchlist; comment bumped.

## Attribution finding (not urgent — logged, not nudged)

`local-wip.json` (generated 2026-08-28T15:45:05Z, ~17h old at scout time —
fresh) carries `author_email_ok` / `last_commit_email_ok` for the first time
since the scanner was updated (`60535f0`, 2026-08-27); the Mac's checkout
finally picked it up. Result: **5 of 6 tracked repos clean** on both flags.
The exception — `sybil` (`ai-capability-app`): `author_email_ok: true` (the
*next* commit here would count fine) but `last_commit_email_ok: false` (the
last one, 2026-06-23, did not). This is the tomgreen.ai-2026-08-27 failure
class, but on a repo dormant since June with nothing queued behind it — no
same-day work at risk, so this doesn't meet the "nudge immediately" bar
(that's for a live misconfig about to eat today's work). Recording it here
for the retro/failsafe: that one commit stays uncountable until someone
runs a history rewrite on it, same recipe as the tomgreen.ai recovery
[[repos/ai-capability-app]] [[ops]].

## Open PRs (org-wide, 4 total — down from 6; `ai-capability-app` #7 merged
2026-08-28)

1. **`tomgreen.ai` #6** — "The solar system becomes the site: no chrome,
   land and click." Draft, opened 2026-08-28, still updated today
   (2026-08-29) — live same-day WIP, not a scout candidate.
2. **`c2-client-matrix` #1** — unchanged: open since 2026-04-21, not a
   draft, 0 comments, no review activity. Still the only non-draft PR
   outside `tomgreen.ai`'s own churn [[repos/c2-client-matrix]]. Dispatch
   contract `2026-08-27-c2cm-review-01` (openai/frontier, review) is already
   queued and unclaimed — the Mac runner (D2) still isn't live, so no new
   contract needed for this candidate.
3. **`ai-capability-app` #4** (draft, docs-only) — unchanged, open since
   2026-04-26 [[repos/ai-capability-app]].
4. **`BrightPaws` #1** (draft) — unchanged, open since 2026-05-03, needs a
   human finish-or-close call, not a quick win [[repos/BrightPaws]].

No open issues anywhere in the org (0 results) — no assigned-issue
candidates today.

## Local WIP (`local-wip.json`, generated 2026-08-28T15:45:05Z — fresh)

All 6 tracked repos: 0 dirty files except `ivy` (1, uncommitted — not a ship
candidate on its own, consistent with prior days), 0 unpushed commits
anywhere. No "push X" candidate today.

## Dispatch queue (unchanged from yesterday, no new contracts emitted)

`dispatch/queue/` holds 3 open contracts, all still unclaimed pending the
D2 Mac runner: `2026-08-27-c2cm-review-01` (review, c2-client-matrix #1),
`2026-08-28-tomgreenai-copy-01` and `2026-08-28-tomgreenai-layout-01`
(both build, tom-created). Today's only non-draft PR (`c2-client-matrix`
#1) already has a live review contract, so no new contract is warranted —
0 of `dispatch.daily_cap` (6) used today.

## Carry-over from yesterday

`c2-client-matrix` #1 still open, still unconverted — same standing
question for the retro on whether repeated nudging is worth continuing
[[repos/c2-client-matrix]].

## Top pick

**`c2-client-matrix` #1 — review and merge.** With yesterday's fresher
candidate (`ai-capability-app` #7) merged, this reverts to the cheapest
real ship available: small, self-tested, ready, sitting unreviewed for
over four months, and already has a queued review contract waiting on the
runner. `ai-capability-app` #4 is next if there's bandwidth (small,
docs-scoped, easy to flip out of draft); `BrightPaws` #1 needs a decision
rather than more nudging.
