# 2026-08-27

## Watchlist sync

14 repos on the account, unchanged in count — but `margaux-en-tutor` is gone
from the API list and `BrightPaws` has appeared in its place (same PR #1,
"Story: Margot the cat catches Tom the mouse," still open/draft, still dated
2026-05-03) — a rename, not a net add/drop. 0 forks, 0 archived, 0 excludes.
`config.yml` watchlist updated: `margaux-en-tutor` → `BrightPaws`.

## ⚠️ Misconfig risk: today's real work isn't attributed to the connected account

`search_commits org:tompulsarlabs committer-date:>=2026-08-26` shows **6 commits
already on `tomgreen.ai` main this morning** (06:42–07:55 local), real
substantive work (MVP refinement, Fable design handoff, "operating field"
feature, home thesis/typography passes) — but every one is authored
`Tom Green <tom@Toms-MacBook-Air.local>`, not the connected
`249609836+tompulsarlabs@users.noreply.github.com` from `config.yml`. Unlike
every prior day's commits (which all resolve a GitHub `author.login`), these
six carry no linked GitHub account at all in the API response — confirming
the email isn't verified/connected. As it stands, **none of today's work
counts toward the graph** (counting rule 2), despite being real, substantial,
and already pushed. This is exactly the misconfig the immutable verification
section warns about, surfacing before check/failsafe rather than after.
Pushed a notification about it — this is a same-day fixable local `git config`
issue on the Mac, not a code problem, and it's cheaper to fix before more
commits pile up on the wrong email.

## Open PRs (org-wide, 3 total — no change since yesterday other than the rename)

1. **`c2-client-matrix` #1** — "Portfolio optimization — 6 decisions from 20
   Apr team feedback." Not a draft, ready-looking. Open since 2026-04-21
   (over 4 months). Third nudge cycle now unconverted (2026-08-24 nudge
   didn't land). Still the only non-draft open PR org-wide.
2. **`BrightPaws` #1** (draft, ex-`margaux-en-tutor`) — "Story: Margot the cat
   catches Tom the mouse." Open since 2026-05-03, no recent activity. (Note:
   `BrightPaws` did land other real work yesterday — PR #4 "Rebrand app as
   BrightPaws English," merged 2026-08-26 23:37 CEST, correctly attributed —
   so the repo is alive, just not on this stale draft.)
3. **`ai-capability-app` #4** (draft, docs-only) — "docs: audit TODOs against
   codebase." Open since 2026-04-26, no recent activity.

No assigned open issues anywhere in the org (`assignee:tompulsarlabs`, 0
results).

## Local WIP (`local-wip.json`, generated 2026-08-26T15:45:04Z — ~15.5h old,
within the 36h staleness window, treated as current, but now superseded by
the `tomgreen.ai` activity found above)

- All 5 tracked repos clean, 0 unpushed commits. `ivy` shows 1 dirty file
  (uncommitted, not pushed) — minor, not a ship-ready candidate on its own.
- No "push X (N unpushed commits)" candidate today — the cheap local-WIP ship
  that came up the last two days is resolved; the Mac scanner just hasn't
  caught up to this morning's `tomgreen.ai` pushes yet (next run 08:45 local
  will refresh this).

## Carry-over from yesterday

`c2-client-matrix` #1 remains open, still unconverted despite three nudge
cycles — may be worth the retro reconsidering whether repeated nudges on
this PR are working, or whether it should be closed/declined instead.

## Top pick

**Fix the git author email on the Mac** (`tom@Toms-MacBook-Air.local` →
the connected noreply address) before today's `tomgreen.ai` work continues —
otherwise a repeat of this morning's 6 commits all miss the graph. This is
today's single highest-leverage action: cheapest to fix now, most expensive
to fix later (would need history rewrite + force-push to recover the 6
already-pushed commits). If that's already handled, `c2-client-matrix` #1
(review and merge, ready since April, third unconverted nudge) is next.
