# 2026-09-05

## Attribution

`local-wip.json` on hand (generated 2026-09-05T06:45:04Z / 08:45 CEST, this
morning's scan — fresh, well under the 36h staleness bar):

- **`tomgreen.ai`** — **new risk, not the stale repeat from 08-31→09-04.**
  Currently checked out on `codex/remove-baked-fragments` (not `main`), last
  commit dated **2026-09-04** (today's own working day, not a months-old
  branch), `last_commit_email_ok: false` while `author_email_ok: true` (the
  *next* commit from this checkout would carry a connected address — only
  the commit already made on this branch is bad). **21 dirty files** sit on
  top of it, uncommitted. The branch name and the day's PR #16 activity
  ("planetary capture engine," opened 12:27 UTC yesterday, still updating
  through 20:49 UTC) both point at live, valuable work in progress on the
  account's highest-volume repo — this is exactly the case the playbook
  says outranks every other candidate and breaks the scout's silent rule:
  cheap to fix now (amend the one bad-author commit before it merges to
  `main`; the branch hasn't landed yet so no history rewrite is needed),
  expensive once it lands [[repos/tomgreen.ai]] [[ops]]. Sent a push
  notification alongside this entry rather than waiting for 18:00.
  The previously-tracked stale flag (`claude/fable-system-design-retro-qrvdfy`,
  last commit 08-29) no longer appears in today's scan — the scanner reports
  the currently-checked-out branch only, and the Mac has since moved off it;
  not evidence that branch was fixed, just that it's no longer what's checked
  out.
- **`talent-radar`** — `author_email_ok: true`, `last_commit_email_ok: true`.
  2 unpushed commits, 3 dirty files. No attribution risk, work waiting to
  ship (see Local WIP).
- **`ivy`** — `last_commit_email_ok: false` on this Mac checkout's last local
  commit (this morning's own `wip: local scan` bot commit, `bot@ivy.invalid`
  — deliberately unconnected, never meant to count), `author_email_ok: true`.
  The harmless recurring case, not a new risk [[ops]].
- `countersign`, `yeva`, `sybil` (`ai-capability-app`), `talent-scout` clean
  on both flags.

No commits landed yet today as of this run (09:09 CEST — too early to
expect any).

## Watchlist sync

`search_repositories user:tompulsarlabs` — **16 repos**, 0 forks, 0
archived, 0 private-visibility surprises. Exact match against `config.yml`'s
current 16-repo list. No additions, no drops, no config change needed today.

## Open PRs (org-wide, 9 total)

1. **`tomgreen.ai` #16** — "The planetary capture engine: the planets are
   the event, and the shards are the plate's." Draft, opened 2026-09-04
   12:27 UTC, updated 20:49 UTC same day — Tom's own live WIP, freshest
   thread on the account. Same repo carrying today's attribution risk
   (above). No dispatch contract yet — queued one today (below).
2. **`talent-radar` #1** — "Restore Cowork history, build the Session 1
   fetch layer, take it live on Supabase, and add the Radar UI." Draft,
   opened 09-03, updated 09-04 08:40 CEST. Paired with 2 unpushed local
   commits on the same repo — likely more of the same work still landing.
   No dispatch contract yet — queued one today (below).
3. **`tomgreen.ai` #15** — "Say the number, the name, or the decision — in
   the header" (`copy-02`). Draft since 09-03. Underlying contract already
   `verified: true` in `dispatch/done/`.
4. **`tomgreen.ai` #14** — "One word for each thing, and the words to stop
   using" (`context-01`). Draft since 09-03. Underlying contract already
   `verified: true` in `dispatch/done/`.
5. **`tomgreen.ai` #13** — "Golden-path VFX asset proof (review gate, not a
   site change)." Draft since 09-02. Review contract `vfx-review-01`
   already `verified: true` in `dispatch/done/`.
6. **`countersign` #1** — "Add REBUILD.md." Draft, opened 08-29. Review
   contract `countersign-review-01` already `verified: true` in
   `dispatch/done/`. Eighth day unreviewed by a human.
7. **`c2-client-matrix` #1** — parked (`config.yml` `watchlist.parked`): no
   candidate, contract, or audit produced [[repos/c2-client-matrix]].
8. **`BrightPaws` #1** — draft, stale since 05-03. Known non-mover
   [[repos/BrightPaws]].
9. **`ai-capability-app` #4** — draft, stale since 04-26. Known non-mover
   [[repos/ai-capability-app]].

No open issues assigned org-wide (`assignee:tompulsarlabs`, 0 results).

## Local WIP

`talent-radar` — **2 unpushed commits, 3 dirty files**, both attribution
flags clean. Cheapest concrete ship available today: push it — advances the
PR #1 thread already open.

`tomgreen.ai` — 21 dirty files on `codex/remove-baked-fragments` (see
Attribution above); 0 unpushed commits (the one bad-author commit is
already pushed to this branch, just not merged to `main`).

All other five scanned repos (`yeva`, `countersign`, `ivy`, `sybil`,
`talent-scout`) show 0 unpushed commits, 0 or already-accounted-for dirty
files.

## Dispatch queue

Empty going into today — all prior contracts resolved to `dispatch/done/`
(6) or `dispatch/failed/` (5), nothing left `open` or stuck `claimed`. The
runner ticked as recently as 2026-09-04T19:07 CEST (`result: idle`,
`lint_ok: true`, both harnesses resolve) — the dirty-clone fix from
yesterday's PR #17 held overnight; no runner blocker today.

Queued **2 new review contracts** (both cross-family: lane `frontier`, pool
`openai`, matching the pattern of every prior review contract regardless of
repo):

- `2026-09-05-tomgreenai-planetary-review-01` — review `tomgreen.ai` PR #16
  on its current head.
- `2026-09-05-talentradar-review-01` — review `talent-radar` PR #1 on its
  current head.

2 of `dispatch.daily_cap` (6) used today. `scripts/dispatch-lint.sh` passes.

## Top pick

**Fix the `tomgreen.ai` attribution risk on `codex/remove-baked-fragments`**
before it merges — one commit to amend, cheap now, expensive once on `main`
(outranks every other candidate per the playbook; pushed as a notification
already). Concrete next-cheapest ship after that: push `talent-radar` (2
unpushed local commits, clean attribution). Standing threads behind both:
`tomgreen.ai` #16 and `talent-radar` #1 now carry review contracts; `#13`,
`#14`, `#15`, and `countersign` #1 await human merge/review decisions on
already-verified work; `c2-client-matrix` #1 stays parked.
