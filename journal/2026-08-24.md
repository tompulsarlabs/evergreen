# 2026-08-24

## Watchlist sync

11 repos on the account, 0 forks, 0 archived, 0 excludes — all 11 synced into
`config.yml` (`watchlist.repos`). This is the first run with a real repo list
(previously just `source: auto` with no synced array).

## Access note

Repo-scoped GitHub MCP tools (`list_branches`, `list_commits`, `get_file_contents`,
etc.) are restricted to `tompulsarlabs/evergreen` only this run — calls against
the other 10 repos returned "not configured for this session." Only the
search-based tools (`search_pull_requests`, `search_issues`, `search_commits`)
reach across the org, and `search_commits` only sees each repo's default branch.
Net effect: **open PRs and assigned issues are fully covered; "branches pushed
recently with no PR" is not verifiable this run** — no repo showed any commit
activity in the last 7 days per `search_commits`, so there's no positive signal
being missed, but a push to a non-default branch would be invisible either way.
Worth a retro note if this access scoping persists.

## Candidates

1. **`c2-client-matrix` PR #1** — "Portfolio optimization — 6 decisions from 20
   Apr team feedback." Not a draft, 0 comments, self-contained test plan already
   written by the author. Open since 2026-04-21 with no review activity since.
   Closest to merge of the three: review it, merge it.
2. **`margaux-en-tutor` PR #1** — "Story: Margot the cat catches Tom the mouse"
   (draft). Manual iPad test plan, all boxes unchecked. Open since 2026-05-03.
3. **`ai-capability-app` PR #4** — "docs: audit TODOs against codebase" (draft,
   docs-only). Surfaces 8 real bugs with a recommended fix sequence; the PR
   itself is trivial to merge but is a planning artifact, not shipped work.

No assigned issues open. No carry-over from yesterday — the one item flagged
("wire the three cloud routines, confirm push reaches the phone") was completed
during yesterday's failsafe/check runs.

## Top pick

**`c2-client-matrix` #1** — review and merge. It's the only non-draft PR, has
no outstanding review, and the author's own test plan is already filled in.
Lowest-effort real contribution available today.
