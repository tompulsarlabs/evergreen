# 2026-08-28

## Watchlist sync

`search_repositories org:tompulsarlabs` shows 14 repos, 0 forks, 0 archived —
identical to the 2026-08-27 list. `config.yml` exclude list is still empty.
No watchlist change to commit; bumped the sync-date comment only.

## Already real work today, before the scout ran

Two PRs opened by the connected account before 09:00 local, both genuine
signal for *today* (Europe/Berlin dates, not UTC):

1. **`ai-capability-app` #7** — "Add Cloud Agent development environment
   config," opened 08:09 CEST via the Cursor app. Not a draft. Adds
   `.cursor/environment.json` + idempotent `install.sh` so a Cloud Agent boots
   straight into a working Next.js dev server, with placeholder Supabase env
   so demo routes need no real backend. Validation is thorough and already
   done: lint clean, build succeeds (27 routes), all public demo routes 200,
   gated route redirects correctly, verified twice against a prebuilt Cloud
   Agent environment. This repo (local alias `sybil`) had been dormant since
   2026-06-23 [[repos/ai-capability-app]] — first real activity in over two
   months.
2. **`tomgreen.ai` #2** — "Recovery review + Wave 0: content restored,
   evidence truthful, transitions refined," opened 00:47 CEST (22:47 UTC
   2026-08-27 — lands on today's date only because Berlin is ahead of UTC)
   via the Claude app. Draft. Large, structured recovery: 5 commits
   (review evidence, truth-first content restore, deleted-route restoration,
   ship-surface fixes, transition refinements), each described as
   individually revertible, plus a 12-section recovery-review report. Explicitly
   gates on two owner decisions before it can leave draft (Home spine wording,
   3D direction approval) — not mergeable without Tom.

Neither shows up in `search_commits committer-date:2026-08-28` (0 raw commits
today so far, only the bot local-wip push at 08:45) — today's signal so far is
PR-opened contributions, not commits.

## Open PRs (org-wide, 6 total — up from 3 yesterday)

1. **`ai-capability-app` #7** (see above) — ready to merge now.
2. **`tomgreen.ai` #2** (see above) — draft, blocked on Tom's two decisions.
3. **`ivy` #2** — "Dispatch: executive layer — D0 design + D1 contract
   plumbing," opened yesterday 16:23 CEST, draft, already counted in
   2026-08-27's total. Blocked on a fact only Tom's machine has (Codex CLI
   model list for the subscription tier) — not actionable from a cloud run.
   Not a new candidate today, listed for completeness.
4. **`c2-client-matrix` #1** — unchanged, still open since 2026-04-21, still
   the only non-draft PR with zero nudge conversion history
   [[repos/c2-client-matrix]].
5. **`BrightPaws` #1** (draft) — unchanged, open since 2026-05-03
   [[repos/BrightPaws]].
6. **`ai-capability-app` #4** (draft, docs-only) — unchanged, open since
   2026-04-26 [[repos/ai-capability-app]].

No assigned open issues anywhere in the org (0 results).

## Local WIP (`local-wip.json`, generated 2026-08-28T06:45:06Z — fresh, ~15 min
old at scout time)

All 5 tracked repos: 0 unpushed commits. `ivy` shows 1 dirty file (uncommitted,
not a ship candidate on its own — same as yesterday). No "push X" candidate
today.

**Note:** this scan still doesn't carry `author_email_ok` /
`last_commit_email_ok`, even though `scripts/local-wip.py` on `main` has
computed them since yesterday's `60535f0` — the Mac's checkout of the script
likely hasn't picked up that commit yet. Not a blocker today (0 unpushed
commits anywhere, so there's nothing local to mis-attribute right now), but
worth the Mac re-pulling `ivy` before it matters.

## Carry-over from yesterday

`c2-client-matrix` #1 still open, still unconverted, still the retro's call on
whether repeated nudging is worth continuing [[repos/c2-client-matrix]].

## Top pick

**`ai-capability-app` #7 — review and merge.** Fully validated (lint, build,
route checks, prebuilt-environment re-verification), self-contained, no owner
decision blocking it, and it revives a repo that had been dormant since June.
Cheapest real ship available today. If more bandwidth: `tomgreen.ai` #2 is
where today's volume is actually concentrated, but it needs Tom's two
decisions (Home spine, 3D direction) before it can move past draft.
