# 2026-08-31

## Attribution finding — urgent, nudged immediately

`local-wip.json` (generated 2026-08-31T06:47:33Z, ~13 min before this scout
run — fresh) flags two repos with `author_email_ok: false`, meaning their
*next* commit would author as a disconnected address and not count (rule 2):

1. **`talent-scout`** — `dirty_files: 1`, `author_email_ok: false`,
   `last_commit_email_ok: false`. There is an uncommitted local change sitting
   behind a misconfigured identity right now — the clearest "about to eat
   today's work" case the playbook's outranking rule describes
   [[repos/talent-scout]]. This repo's only prior real commit
   (2026-08-29, `061f5243`) already needed a resolvability fix for the same
   reason [[ops]].
2. **`tomgreen.ai`** — `author_email_ok: false`, `last_commit_email_ok:
   false`, local checkout sitting on `claude/fable-system-design-retro-qrvdfy`
   (not `main`). `dirty_files: 0`, `unpushed_commits: 0`, so nothing is
   queued behind it at this instant — same shape as the below-the-bar `sybil`
   case the 2026-08-30 retro validated [[repos/ai-capability-app]]. Flagging
   it anyway rather than applying that carve-out: this is the account's
   highest-volume repo, mid-cycle on an open PR (#10, below), and it is the
   *exact* repo and failure mode that produced the 2026-08-27 six-commit
   history rewrite [[repos/tomgreen.ai]] [[ops]]. The cost of an unneeded
   nudge is a push notification; the cost of missing a live recurrence is
   another public rewrite.

Both are a one-line `git config user.email` fix while nothing has been
pushed. Nudged now via PushNotification rather than waiting for the 18:00
check, per the playbook's outranking rule.

## Watchlist sync

`search_repositories org:tompulsarlabs` — **15 repos**, 0 forks, 0 archived.
Same 15 names as `config.yml`'s current list. No watchlist edit needed.

## Open PRs (org-wide, 5 total)

1. **`tomgreen.ai` #10** — "One object, two layers: a plain portfolio, and a
   world behind the moon." Open (not draft), opened 2026-08-30T18:33 CEST,
   last updated 20:03 CEST, carried over from yesterday's failsafe note
   [cite:2026-08-30]. Ready for review/merge — the cheapest fresh ship today
   once the attribution fix above is out of the way, matching this repo's
   pattern of same/next-day merges (#6–#9 all merged within a day of
   opening) [[repos/tomgreen.ai]].
2. **`countersign` #1** — "Add REBUILD.md: shareable spec to rebuild
   Countersign from scratch." Draft, opened 2026-08-29, no activity since —
   still same-day WIP rather than a converted candidate
   [[repos/countersign]].
3. **`c2-client-matrix` #1** — unchanged: open since 2026-04-21, not a
   draft, 0 comments. The standing candidate that has never converted;
   review contract `2026-08-27-c2cm-review-01` remains queued and unclaimed
   [[repos/c2-client-matrix]].
4. **`BrightPaws` #1** — draft, stale since 2026-05-03, no recent activity.
   Known non-mover; the repo itself is alive via other PRs, not this one
   [[repos/BrightPaws]].
5. **`ai-capability-app` #4** — draft, stale since 2026-04-26, docs-only
   TODO audit. Known non-mover, ranked last of the standing PRs
   [[repos/ai-capability-app]].

No open issues anywhere in the org (`assignee:tompulsarlabs`, 0 results) —
no assigned-issue candidates today.

## Local WIP (`local-wip.json`, generated 2026-08-31T06:47:33Z — fresh)

Seven tracked repos. Besides the two attribution findings above:
`talent-radar`, `yeva`, `countersign` clean on both flags, 0 unpushed
commits. `sybil` (`ai-capability-app`) unchanged from prior scans —
`author_email_ok: false`, `last_commit_email_ok: false` on its one commit
from 2026-06-23, still dormant with nothing queued, still below the
nudge-immediately bar [[repos/ai-capability-app]].

`ivy`'s own entry shows `dirty_files: 1`, `last_commit_email_ok: false`, but
`author_email_ok: true` — the next commit from that checkout would be fine.
This is odd against this cloud clone's view of `origin/main`, whose tip
non-bot commit (`020cb0c`, 2026-08-30) carries the connected address
cleanly. Recording the discrepancy rather than resolving it — it isn't
verifiable from the cloud sandbox, and `author_email_ok: true` means it
doesn't meet the outranking bar regardless [[ops]].

## Dispatch queue (unchanged, no new contracts emitted)

`dispatch/queue/` still holds the same 3 unclaimed contracts from
2026-08-27→28 (`2026-08-27-c2cm-review-01`, `2026-08-28-tomgreenai-copy-01`,
`2026-08-28-tomgreenai-layout-01`), none expired, none claimed — the D2 Mac
runner still isn't live [[patterns]]. No new contract today: `tomgreen.ai`
#10 is a same-day, likely self-merged PR rather than a review target;
`countersign` #1 is same-day WIP; `BrightPaws` #1 needs a human
finish-or-close call; `ai-capability-app` #4 is a one-click merge, not a
build/review task. Adding more inventory to an already-stalled queue would
be waste, not progress, per the retro's fleet-level framing [[patterns]].
0 of `dispatch.daily_cap` (6) used today.

## Carry-over from yesterday

`tomgreen.ai` #10 (opened 20:33 CEST 2026-08-30, still open) and
`c2-client-matrix` #1 (unconverted since April) both carry over unresolved
[[repos/tomgreen.ai]] [[repos/c2-client-matrix]].

## Top pick

**Fix `talent-scout` and `tomgreen.ai`'s Mac git identity before either's
next commit.** Outranks every other candidate per the playbook's attribution
rule — one `git config user.email` line now versus a public history rewrite
once real work lands on a disconnected address, exactly what happened to
`tomgreen.ai` on 2026-08-27. `talent-scout` has real uncommitted work behind
it right now. Next after that: `tomgreen.ai` #10 — review and merge, the
cheapest fresh ship once identity is fixed.
