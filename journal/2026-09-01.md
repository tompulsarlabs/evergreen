# 2026-09-01

## Attribution finding — persisting, second day unconverted

`local-wip.json` (generated 2026-08-31T17:45:02Z, ~15h15m before this scout
run — inside the 36h freshness window, no fresher scan has landed yet since
the Mac's 08:45 CEST scan for today hasn't been pushed) still flags the same
two repos from yesterday's same-morning nudge, neither fixed:

1. **`talent-scout`** — `author_email_ok: false`, `last_commit_email_ok:
   false`, and `dirty_files` grew from 1 to 2 over yesterday — more
   uncommitted real work now sitting behind the bad identity than when the
   scout nudged yesterday morning [[repos/talent-scout]]. Still unpushed, so
   nothing uncountable has landed on `main` yet, but the pile behind the
   misconfigured address is growing, not shrinking.
2. **`tomgreen.ai`** — unchanged from yesterday: `author_email_ok: false`,
   `last_commit_email_ok: false`, local checkout still on
   `claude/fable-system-design-retro-qrvdfy` rather than `main`, `dirty_files:
   0` / `unpushed_commits: 0` (nothing queued at this instant). Flagged again
   anyway per yesterday's reasoning — this is the exact repo and failure mode
   behind the 2026-08-27 six-commit history rewrite [[repos/tomgreen.ai]]
   [[ops]].

Both are still a one-line `git config user.email` fix while nothing has
reached `main`. Nudging again via PushNotification rather than waiting for
the 18:00 check, per the playbook's outranking rule — this is now day 2 of
an unconverted attribution risk with growing uncommitted work behind it on
`talent-scout`.

## Watchlist sync

`search_repositories org:tompulsarlabs` — **15 repos**, 0 forks, 0 archived.
Same 15 names as `config.yml`'s current list. No watchlist edit needed.

## Open PRs (org-wide, 5 total, unchanged from yesterday)

1. **`tomgreen.ai` #10** — "One object, two layers: a plain portfolio, and a
   world behind the moon." Open (not draft), opened 2026-08-30T18:33 CEST,
   last updated 2026-08-30T21:03 CEST — no new activity since yesterday's
   scout read, now carrying over a second full day
   [cite:2026-08-31] [[repos/tomgreen.ai]]. Cheapest fresh ship today once
   the attribution fix above is out of the way.
2. **`countersign` #1** — draft, opened 2026-08-29, still no activity since
   — same-day WIP, not yet converted [[repos/countersign]].
3. **`c2-client-matrix` #1** — unchanged: open since 2026-04-21, not a
   draft, 0 comments. The standing never-converted candidate; review
   contract `2026-08-27-c2cm-review-01` remains queued and unclaimed
   [[repos/c2-client-matrix]].
4. **`BrightPaws` #1** — draft, stale since 2026-05-03, no recent activity.
   Known non-mover.
5. **`ai-capability-app` #4** — draft, stale since 2026-04-26, docs-only
   TODO audit. Known non-mover, ranked last of the standing PRs
   [[repos/ai-capability-app]].

No open issues anywhere in the org (`assignee:tompulsarlabs`, 0 results) —
no assigned-issue candidates today. No new commits or merged PRs org-wide
for 2026-09-01 as of this run (09:08 CEST — too early in the day to expect
any).

## Local WIP (`local-wip.json`, generated 2026-08-31T17:45:02Z — fresh, but
last night's scan, not this morning's)

Seven tracked repos. Besides the two attribution findings above:
`talent-radar`, `yeva`, `countersign` clean on both flags, 0 unpushed
commits. `sybil` (`ai-capability-app`) unchanged — `author_email_ok: false`
on its one commit from 2026-06-23, still dormant with nothing queued, still
below the nudge-immediately bar [[repos/ai-capability-app]].

`ivy`'s own entry still shows `dirty_files: 1`, `last_commit_email_ok:
false`, `author_email_ok: true` — same discrepancy noted yesterday against
this cloud clone's clean `origin/main` tip. Not independently verifiable
from the cloud sandbox, and `author_email_ok: true` means it doesn't meet
the outranking bar regardless [[ops]].

## Dispatch queue (unchanged, no new contracts emitted)

`dispatch/queue/` still holds the same 3 unclaimed contracts from
2026-08-27→28 (`2026-08-27-c2cm-review-01`, `2026-08-28-tomgreenai-copy-01`,
`2026-08-28-tomgreenai-layout-01`), none expired, none claimed — the D2 Mac
runner still isn't live [[patterns]]. No new contract today: nothing new
converted since yesterday to justify one, and adding inventory to an
already-stalled queue is waste, not progress [[patterns]]. 0 of
`dispatch.daily_cap` (6) used today.

## Carry-over from yesterday

`tomgreen.ai` #10 (open a second full day) and `c2-client-matrix` #1
(unconverted since April) both carry over unresolved
[[repos/tomgreen.ai]] [[repos/c2-client-matrix]]. The attribution finding
above also carries over, now with growing uncommitted work behind it on
`talent-scout`.

## Top pick

**Fix `talent-scout`'s Mac git identity before its next commit.** Outranks
every other candidate per the playbook's attribution rule, and more urgent
today than yesterday: the pile of uncommitted work sitting behind the bad
address grew from 1 to 2 dirty files overnight while the identity stayed
broken. `tomgreen.ai`'s identity is the same unfixed risk but with nothing
freshly queued behind it. Next after that: `tomgreen.ai` #10 — review and
merge, now two days old and still the cheapest fresh ship once identity is
fixed.

## Ship — talent-scout warm pipeline, Phase 0 core (Tom)

Shipped 11 commits to `tompulsarlabs/talent-scout` today
(`ca0b28a..c48f879`), pivoting Scout from requisition-triggered sourcing to a
warm pipeline: map the best people ahead of need, then orchestrate the
company's extended orbit (TA, founders, investors, board advisors) to build
real relationships before there is a role to pitch [[repos/talent-scout]].

Ran `/plan-ceo-review` on the concept first, plus an adversarial pass and a
Codex outside voice. That produced 19 settled decisions and killed two of my
own ideas: a monthly map-diff that can't tell a real job change from model
variance, and a candidate transparency page that was trying to be a legal
notice and a conversion mechanic on incompatible timing.

Landed: append-only relationship ledger, identity spine (`Person` durable,
`CandidateProfile` recomputed), per-org tenancy, verification gate,
untrusted-content boundary for draft generation, send gate with kill switch
and suppression, signed links, Article 14 notice page, Postgres migration,
and a DPIA skeleton for counsel. 140 tests, from zero test infrastructure
this morning.

Blocked on me, not on the work: Supabase provisioning (billable, my call) and
the DPIA (counsel). Both gate Phase 0 touching real people's data.

## Attribution flag — resolved, and why it kept firing

The scout's day-2 nudge on `talent-scout` is stale. `git config user.email`
is `tom@pulsarlabsai.com` at both local and global scope, and all 11 of
today's commits are authored and committed as `tompulsarlabs
<tom@pulsarlabsai.com>`. The dirty files it was worried about are now
committed and pushed, so the pile behind the flag is gone [[ops]].

What the scanner is almost certainly seeing: two commits from 2026-06-19
(`106c539`, `069c55e`) carry `Tom Green <tom@tomgreen.ai>` in history. That
is real but historical, and `061f524` already re-triggered deployment with a
resolvable author, so Vercel is fine. Not rewriting published history to
clear a cosmetic flag — the 2026-08-27 `tomgreen.ai` six-commit rewrite is
the cautionary case.

Worth a scanner change: `author_email_ok` should grade the *configured
identity* and the *tip commit*, not the whole history. As written it can
never go green on any repo with a bad commit in its past, which makes it a
permanent false positive and trains me to ignore it [[patterns]].
