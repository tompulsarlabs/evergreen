# 2026-09-03

## Attribution — nothing new, one repeat below the bar

`local-wip.json` on hand (generated 2026-09-02T17:45:05 CEST — yesterday's
afternoon scan, 15h old, under the 36h staleness bar, this morning's 08:45
scan had not landed as of this run):

- **`tomgreen.ai`** — `author_email_ok: true` (next commit fine),
  `last_commit_email_ok: false` on the tip of
  `claude/fable-system-design-retro-qrvdfy` (stale, last commit 2026-08-29).
  Same repeat noted 2026-08-31 through 09-02: `dirty_files: 0` /
  `unpushed_commits: 0`, nothing queued behind it, and that branch isn't
  `main` so it wouldn't count regardless of author. Below the nudge bar
  again; not re-flagging [[repos/tomgreen.ai]].
- All other tracked repos (`talent-radar`, `yeva`, `countersign`, `sybil`,
  `talent-scout`) clean on both flags. `ivy`'s own Mac checkout: 1 dirty
  file, 0 unpushed, both email flags true — same harmless discrepancy noted
  on prior days [[ops]].

## Watchlist sync

`search_repositories org:tompulsarlabs` — **16 repos**, 0 forks, 0 archived.
One new repo since the 2026-08-29 sync: **`tompulsarlabs/tompulsarlabs`**
(the org profile repo, created 2026-09-02, first real signal already
recorded [[repos/tompulsarlabs]]) — present in the org but missing from
`config.yml`'s watchlist. Added it today; watchlist is now 16 repos, 0
excludes.

## Open PRs (org-wide, 5 total)

1. **`tomgreen.ai` #13** — "Golden-path VFX asset proof (review gate, not a
   site change)." Draft, opened 2026-09-02T20:35 CEST, last updated
   2026-09-02T22:27 CEST — replaces #12 (merged yesterday) as the freshest
   thread on the account, on the highest-volume repo [[repos/tomgreen.ai]].
   The title claims it's inert (a review gate, not a functional change);
   queued a review contract today to check that claim against the diff
   (below) since the PR body isn't inspectable from this cloud session
   [[ops]]. Today's cheapest real ship once it comes out of draft and
   merges.
2. **`countersign` #1** — "Add REBUILD.md." Draft, opened 2026-08-29, still
   no review activity from a human since; the dispatch review contract
   already executed 2026-09-02 (verdict: keep in draft, late-webhook race
   found) without moving it [[repos/countersign]]. Sixth day unreviewed by
   a human.
3. **`c2-client-matrix` #1** — unchanged: open since 2026-04-21, not a
   draft, 0 comments. Standing never-converted candidate, per the
   2026-08-30 retro decision not closing or demoting yet
   [[repos/c2-client-matrix]].
4. **`BrightPaws` #1** — draft, stale since 2026-05-03. Known non-mover.
5. **`ai-capability-app` #4** — draft, stale since 2026-04-26, docs-only
   TODO audit. Known non-mover, ranked last [[repos/ai-capability-app]].

No open issues assigned org-wide (`assignee:tompulsarlabs`, 0 results). No
commits yet today as of this run (09:07 CEST — too early to expect any
beyond a Mac local-wip scan that hasn't landed yet).

## Local WIP

Covered under Attribution above — no "push X" candidate today, all seven
tracked repos show 0 unpushed commits in the scan on hand.

## Dispatch queue

Four `build` contracts opened yesterday evening by the 18:00 check
(`created: 2026-09-02T18:30`) are still `state: open`, unclaimed, in
`dispatch/queue/`: `2026-09-02-tomgreenai-context-01` (CONTEXT.md
glossary), `-copy-02`, `-layout-02`, and `-photo-02` (re-queues of three
contracts the runner had refused 2026-09-01 on the now-fixed attribution
gate bug [[ops]]). All expire 2026-09-04T18:30, well inside today's
09:15-21:00 runner window — not yet a blocker, just not yet claimed.
`dispatch/done/` and `dispatch/failed/` unchanged since yesterday.

With 0/6 of `dispatch.daily_cap` used today, queued one new contract:
**`2026-09-03-tomgreenai-vfx-review-01`** (review, frontier/openai —
cross-family, tomgreen.ai's build work is Claude-session-authored) —
review PR #13 against its own "review gate, not a site change" claim.
`scripts/dispatch-lint.sh` passes (10 contracts, schema and cap hold).

## Top pick

**`tomgreen.ai` #13 — bring out of draft and merge**, once the queued
review contract confirms the diff matches its own "not a site change"
claim. Freshest real thread on the account, no attribution or dispatch
blocker in the way. Next after that: the four queued `tomgreen.ai` build
contracts landing via the runner today, and `c2-client-matrix` #1 as the
standing fallback if nothing else moves.
